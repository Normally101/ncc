# File interni che NON devono finire sul sito pubblico (Vercel).
# Restano nel repo privato (backup) ma esclusi dal deploy → 404 sul sito.
# ⚠️ Il sito è su VERCEL (non GitHub Pages), deploy da branch main.

# Migrazioni SQL — espongono l'architettura di sicurezza (RLS, anti-cheat, rate-limit)
*.sql

# Documentazione interna (architettura, handoff, decisioni)
*.md

# Script interni
*.py
*.sh
# Utilita' di sviluppo eseguite a mano (stato progetto, semplificazione geo).
# Fino al 23/08 scripts/stato-progetto.mjs finiva sul sito pubblico: innocuo,
# ma non c'e' ragione perche' ci sia.
scripts

# Edge Functions server-side (codice backend, mai pubblico)
supabase

# Cartelle interne / tooling
docs
.github
.agents
.claude
skills
_mockups

# Suite di test (node --test) — logica interna di QA, non serve pubblica
test
test-support

# Save di gioco (struttura dati interna / potenziale PII futura) — non servono pubblici
slot_*.json
