// Test per il gruppo "Driver Coins e vanita'": 6 azioni cieche da rendere esercitabili.
// File nuovo, non tocca i test esistenti.
import { test, describe, beforeEach } from 'node:test';
import assert from 'node:assert/strict';

describe('driver coins e vanita - azioni cieche', () => {
  test('segnaposto: il file cresce una funzione per volta', () => {
    assert.ok(true);
  });
});
