# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
*.suo
*.user

# Claude Code local config
.claude/
.superpowers/
.agents/
skills-lock.json

# Local databases
ruvector.db

# Node (if ever used)
node_modules/

# Design mockups / previews — NON servono in produzione e GitHub Pages
# li servirebbe pubblicamente. Tenuti solo in locale.
_mockups/

# Save files ARE tracked intentionally for cross-device sync:
# slot_1.json, slot_2.json, slot_3.json → commit these!

# Dev preview pages — non servire pubblicamente
preview-*.html

# Configurazione locale estensione Continue (contiene il projectId Google Cloud)
.continue/

# Copia di lavoro del GeoJSON ISTAT (2,75 MB) — la rigenera scripts/semplifica-geo.mjs
scripts/.cache/
